<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uwu.study.db.info.dao.CustomerDao">

    <!-- 创建表SQL，参数为表名 -->
    <update id="createCustomerTable" parameterType="string">
        CREATE TABLE ${tableName} (
            `customer_id` bigint(20) NOT NULL COMMENT '客户id',
            `name` varchar(64) CHARACTER SET utf8mb4 COLLATE utf8mb4_bin DEFAULT NULL COMMENT '用户名',
            `score` int(4) NOT NULL DEFAULT '0' COMMENT '分数',
            PRIMARY KEY (`customer_id`)
        ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户客户表';
    </update>
</mapper>